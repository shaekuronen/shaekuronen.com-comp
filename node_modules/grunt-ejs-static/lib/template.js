
module.exports = exports = {

  // prepare template for rendering
  prepare_template: function(layout, path_to_file, ejs_static) {

    // make the template file
    ejs_static.grunt.file.write(path_to_file, layout);
    ejs_static.grunt.log.debug('Created file at ' + path_to_file);

    // get the template
    var the_template = ejs_static.grunt.file.read(path_to_file);
    ejs_static.grunt.log.debug('Read template from ' + path_to_file);

    return the_template;

  }
  // end prepare template for rendering

};